import type { Meta, StoryObj } from '@storybook/react';

import { EditArea } from './EditArea.component';

import './EditArea.component.scss'

const meta: Meta<typeof EditArea> = {
    component: EditArea,
};

export default meta;


type Story = StoryObj<typeof EditArea>;

export const Primary: Story = {
  
};